name: reusable workflow test

on:
  issues:
    types: [opened, reopened, closed]

jobs:
  test:
    uses: jsnerdic/reusable-workflows/.github/workflows/messageAction.yaml@main
    permissions:
      contents: read
      issues: write
    with:
      message: 'Hello world!'
  test2:
    needs: [test]
    if: ${{ needs.test.outputs.OUTPUT_TEST != '' }}
    uses: jsnerdic/reusable-workflows/.github/workflows/messageAction.yaml@main
    permissions:
      contents: read
      issues: write
    with:
      message: '<grumpy mumblig>'
  test3:
    needs: [test2]
    if: ${{ needs.test2.outputs.OUTPUT_TEST == '' }}
    uses: jsnerdic/reusable-workflows/.github/workflows/messageAction.yaml@main
    permissions:
      contents: read
      issues: write
    with:
      message: 'Hello back! :wave:'
